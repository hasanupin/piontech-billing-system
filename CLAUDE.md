# Piontech Billing System

Laravel 13 + Filament 5 admin panel. **UI dibangun sepenuhnya dengan Filament** — bukan jQuery/AdminLTE/Blade manual.

## Architecture Reference

Struktur aplikasi ini mengadopsi pola dari project **sippw** (`/Users/hasanupin/www/freelance/sippw`). Dokumen desain lengkapnya ada di:

- `sippw/.plan/SYSTEM_DESIGN.md` — desain sistem menyeluruh
- `sippw/.plan/modules/*.md` — spesifikasi per modul (auth, master data, reports, dashboard)

Yang diadopsi adalah **strukturnya** (layering, konvensi, pola scoping), **bukan domain-nya**. Domain sippw (usbuiyah/pengamal/wilayah) tidak berlaku di sini; domain project ini adalah billing.

Saat membuat modul baru, ikuti konvensi di bawah dan lihat modul **Users** (`app/Filament/Resources/Users/`) sebagai contoh implementasi rujukan.

## Tech Stack

| Layer | Technology |
|---|---|
| Backend | Laravel 13 (PHP 8.3+) |
| Admin UI | Filament 5 (panel `admin`, path `/admin`) |
| Database | MySQL 8 (docker, host port 3309) · SQLite `:memory:` (test) |
| Cache/Queue | Redis (docker, host port 6382) · database driver (queue/cache/session) |
| Dev Environment | Docker Compose (lihat `README-Docker.md`) atau `php artisan serve` terhubung ke MySQL/Redis docker |

## Commands

```bash
php artisan serve                    # dev server lokal (mysql & redis dari docker-compose, host port 3309/6382)
php artisan migrate:fresh --seed     # reset DB + super admin
php artisan test                     # test suite
php artisan make:filament-resource X --generate [--soft-deletes]  # scaffold resource
docker-compose up -d --build         # full stack: app :8002, mysql :3309, redis :6382

npm ci && npm run build              # WAJIB — panel pakai ->viteTheme(); tanpa build panel tampil tanpa CSS
npm run dev                          # hot reload saat menggarap theme.css
```

Login seeder: `admin@example.com` / `password` (role `super_admin`).

> **Asset build tidak opsional.** Panel `admin` mendaftarkan `->viteTheme('resources/css/filament/admin/theme.css')`,
> jadi setiap deploy harus menjalankan `npm ci && npm run build` (sudah masuk `Dockerfile`).
> Di test hal ini ditangani `withoutVite()` pada `tests/TestCase.php` — jangan dihapus.

## Struktur & Konvensi (diadopsi dari sippw)

### Layering

```
Filament Resource / Page     → HTTP + UI layer (setara Controller di sippw; tetap tipis)
     ↓
Service (app/Services)       → business logic non-trivial + DB transaction
     ↓
Model (app/Models)           → representasi tabel, relationships, casts
```

- Resource Filament = pengganti Controller+FormRequest+Blade+DataTables sippw. Validasi ada di form schema, listing di table schema.
- Logika bisnis yang lebih dari sekadar CRUD dipindah ke Service class. Interface hanya untuk service kompleks.
- Service di-bind di `AppServiceProvider::register()` (singleton bila stateless).

### Database & Migrations

- **Semua identifier database & enum dalam Bahasa Inggris** (tabel, kolom, nilai enum, class enum). Bahasa Indonesia hanya untuk display via i18n (`__()` + `lang/id.json`). Contoh: tabel `customers` bukan `pelanggan`, status `suspended` bukan `isolir`.
- Kolom status aktif: `is_active` boolean (default sesuai kebutuhan modul).
- Tabel domain billing memakai **ULID PK** (`$table->ulid('id')->primary()` + trait `HasUlids`); tabel `users` tetap bigint.
- Audit pembuat record: `created_by` FK → `users.id`, `nullOnDelete`, diisi otomatis via `mutateFormDataBeforeCreate`.
- Soft deletes untuk entitas penting (users, dan entitas transaksional nanti).
- Entitas yang tampil di URL publik memakai `uuid` (unique) sebagai route key — lihat pola `getRouteKeyName()` + generate di `booted()` (contoh di sippw: `Usbuiyah`, `Pengamal`).
- Kolom pilihan terbatas pakai `$table->enum(...)` dengan nilai UPPERCASE (`AKTIF`, `SUDAH`) atau snake_case untuk role.

### Enums

- Lokasi: `app/Enums/`, PHP backed enum (string).
- Implement `Filament\Support\Contracts\HasLabel` (dan `HasColor` bila tampil sebagai badge) supaya langsung dipakai `Select::options(Enum::class)` dan `TextColumn::badge()`.
- Contoh: `App\Enums\Role`.

### Models

- Laravel 13 style: atribut `#[Fillable([...])]` dan `#[Hidden([...])]`, bukan properti `$fillable`.
- Casts lewat method `casts()`; enum di-cast ke class enum-nya.
- Relationships selalu dengan return type (`BelongsTo`, `HasMany`, dst).
- Relasi ke user pembuat: `creator()` (`->withTrashed()`).
- Role helpers di `User`: `isRole(Role ...$roles)`, `isSuperAdmin()`.

### Roles & Scoping

Roles (sesuai PRD): `super_admin` (CEO), `admin` (admin penagihan), `field_officer` (petugas lapangan) di `App\Enums\Role`.

Pola sippw yang wajib dipertahankan:

1. **Semua data-scoping berbasis role terpusat di `App\Services\ScopeService`** (singleton). Resource/Service lain tidak boleh berisi logika scoping sendiri.
2. Setiap method scoping berbentuk `match ($user->role) { ... }` dengan default `whereRaw('1 = 0')` (deny by default).
3. Filament resource menerapkan scope di `getEloquentQuery()` — lihat `UserResource::getEloquentQuery()`.
4. Otorisasi tulis (create/update terhadap entitas di luar scope) juga lewat method `authorize*()` di ScopeService (`abort(403)`).
5. Matriks permission per-fitur × per-role didokumentasikan per modul — template: `sippw/.plan/modules/00-overview.md` bagian "Roles & Permissions Matrix".

### Filament Resources

Struktur hasil generator dipertahankan (jangan digabung ke satu file):

```
app/Filament/Resources/{Entity}/
├── {Entity}Resource.php          # model, nav, access gate, scoped query
├── Schemas/{Entity}Form.php      # form schema
├── Tables/{Entity}sTable.php     # table schema (kolom, filter, actions)
└── Pages/                        # List / Create / Edit
```

Konvensi resource:

- **Access gate**: `canAccess()` memeriksa role (contoh: UserResource hanya `super_admin`). Menu otomatis tersembunyi bila tidak punya akses.
- **Navigation group**: master data di grup `Master`; modul domain billing nanti pakai grup sendiri (mis. `Billing`).
- **Password field**: required saat create, kosong = tidak diubah saat edit (`->required(fn ($operation) => $operation === 'create')->dehydrated(fn ($state) => filled($state))`). Hash otomatis via cast `hashed`.
- **created_by**: diisi di `mutateFormDataBeforeCreate` pada page Create.
- **Soft-delete**: `TrashedFilter` + `Restore/ForceDelete` actions; `getEloquentQuery()` dan `getRecordRouteBindingEloquentQuery()` melepas `SoftDeletingScope`.
- **Proteksi diri**: user tidak bisa menghapus akunnya sendiri (lihat `UsersTable` dan `EditUser`).
- **Label kolom/field wajib lewat `__()`** dengan key Bahasa Inggris (`->label(__('Full Name'))`); terjemahan Indonesia di `lang/id.json`. Berlaku juga untuk `getLabel()` enum, `getModelLabel()`/`getPluralModelLabel()`/`getNavigationGroup()` resource. Ganti bahasa aplikasi = ubah `APP_LOCALE` di `.env`.

### Auth

- **Satu halaman login untuk semua role** di `/admin/login` (alias `/login`; `/petugas/login` ikut redirect ke sana) — lihat baris "Panel mobile Petugas" di tabel modul. Kredensial email + password.
- `User` implement `Filament\Models\Contracts\FilamentUser`; `canAccessPanel()` bercabang per panel (`field` = hanya `field_officer`, selain itu = non-petugas) dan menolak user `is_active = false` (setara pengecekan login sippw). Bila nanti ada entitas organisasi (kantor/cabang), tambahkan pengecekan entitas nonaktif di sini juga.

### Testing

- Setiap modul minimal punya feature test kontrol akses: role yang boleh → 200, role lain → 403, guest → redirect login, user nonaktif → 403. Contoh: `tests/Feature/UserModuleTest.php`.
- **Nama method test camelCase** (`testSuperAdminCanAccessUsersResource`), bukan snake_case. Preset Laravel-nya Pint memaksa snake_case, jadi `pint.json` meng-override `php_unit_method_casing` ke `camel_case` — jangan dihapus.
- Factory menyediakan state per-role (`superAdmin()`, `admin()`, `fieldOfficer()`, `inactive()` di `UserFactory`) dan state per-status domain (mis. `suspended()` di `CustomerFactory`).

## Implemented Modules

| Module | Status | Files |
|---|---|---|
| Auth (Filament login + is_active gate) | ✅ | `app/Models/User.php` |
| Users (CRUD, role, soft delete) | ✅ | `app/Filament/Resources/Users/`, `app/Enums/Role.php`, `app/Services/ScopeService.php`, migration `2026_07_17_000003` |
| DB Schema billing — TASK-02 (packages, clusters, customers, transactions, officer_deposits + factories, seeders) | ✅ | migrations `2026_07_18_00000{1..5}`, `app/Models/{Package,Cluster,Customer,Transaction,OfficerDeposit}.php`, `database/seeders/`, `tests/Feature/DatabaseStructureTest.php` |
| Business rules — TASK-03 (enums status/payment, global scope cluster, auto-status + transfer nullify, BillingService) | ✅ | `app/Enums/{CustomerStatus,TransactionStatus,PaymentMethod}.php`, `app/Models/{Customer,Transaction}.php`, `app/Services/BillingService.php`, `ScopeService::scopeCustomersForUser`, `tests/Feature/{TransactionRules,CustomerScope,BillingService}Test.php` |
| RBAC — TASK-04 (super_admin full access via `Gate::before`, 6 policies) | ✅ | `app/Policies/`, `app/Providers/AppServiceProvider.php`, `tests/Feature/RoleAccessTest.php` |
| Master Paket — TASK-05 (CRUD, view-only nihil untuk super_admin/admin, petugas forbidden) | ✅ | `app/Filament/Resources/Packages/`, `app/Policies/PackagePolicy.php`, `tests/Feature/PackageModuleTest.php` |
| Master Cluster — TASK-06 (CRUD admin-only, officer PIC select, customers_count, CustomerRelationManager view-only, ganti PIC → scope pelanggan ikut) | ✅ | `app/Filament/Resources/Clusters/`, `tests/Feature/ClusterModuleTest.php` |
| Pelanggan — TASK-07 (CRUD 4 section, kolom setara Excel, WA/Maps clickable, quick action ISOLIR/Pulihkan admin-only, filter due_today, scope petugas) | ✅ | `app/Filament/Resources/Customers/`, `tests/Feature/CustomerModuleTest.php` |
| Pelanggan — petugas create+edit (di luar TASK; **menyimpang dari PRD §6** atas permintaan pemilik produk: petugas boleh daftar & edit pelanggan **di clusternya sendiri** tanpa lapor admin. Dropdown cluster difilter + prefill via `ScopeService::scopeClustersForUser`, guard server-side `authorizeCustomerCluster` di page Create/Edit. Field **status**, hapus, ISOLIR/Pulihkan, dan import Excel tetap admin-only) | ✅ | `app/Policies/CustomerPolicy.php`, `app/Services/ScopeService.php`, `app/Filament/Resources/Customers/{Schemas/CustomerForm,Pages/CreateCustomer,Pages/EditCustomer}.php`, `tests/Feature/{CustomerModuleTest,RoleAccessTest}.php` |
| Transaksi — TASK-08 (form reaktif prefill paket editable, metode per-role, officer hidden saat transfer, tolak duplikat, page Tagihan Bulan Ini; revisi filter list: default Periode & Rentang Tanggal Bayar = bulan berjalan, toggle LUNAS/TUNAI/TRANSFER diganti dropdown Status + Metode Bayar, tambah dropdown Petugas & Cluster (via `whereHas('customer')`, opsi cluster ikut `ScopeService`), layout filter 1 kolom collapsible (default tersembunyi); Periode & rentang tanggal digabung satu filter ber-checkbox "Gunakan Rentang Tanggal" — unchecked = filter kolom `period` (default bulan berjalan), checked = filter `paid_at` dari–sampai) | ✅ | `app/Filament/Resources/Transactions/`, `app/Filament/Pages/MonthlyBilling.php`, `ScopeService::scopeTransactionsForUser`, `tests/Feature/TransactionModuleTest.php` |
| Tagihan Bulanan — revisi konteks lapangan (di luar TASK: kolom alamat/WA/status pelanggan, tombol ikon Foto Rumah (modal) & Cek Lokasi (Maps) yang **disabled** bila datanya kosong; filter Periode/Cluster/Status Bayar kini **filter halaman** (`HasFiltersForm`, seperti Dashboard) sehingga tata letaknya filter → chart → tabel dan satu filter menggerakkan keduanya; urutan konten diatur lewat `content()` schema (`EmbeddedSchema` + `Grid` widget + `EmbeddedTable`), bukan blade custom; widget = **4 kartu** (jumlah lunas/tidak lunas · nominal belum/sudah bayar · sudah disetor · **Komisi** = `BillingService::commissionTotal()`, hanya CEO+admin dan sengaja mengabaikan filter cluster) + **3 pie** (Total Tagihan: terbayar/belum · Metode Bayar: tunai/transfer · Setoran: sudah/belum disetor; base class `MonthlyBillingPieChart` — doughnut Rp + tooltip rupiah) ikut filter Periode & Cluster tapi **sengaja mengabaikan** filter Status Bayar. **Definisi "harus disetor" = nominal tagihan − transfer** (semua tagihan yang tidak masuk via transfer wajib ditarik petugas lalu disetor), sehingga "belum disetor" = uang yang belum sampai ke admin termasuk yang belum tertagih — **berbeda** dari `held_by_officers` (`tunai − disetor`) yang dipakai Dashboard/strip status/laporan; keduanya sengaja berdampingan, jangan disatukan) | ✅ | `app/Filament/Pages/MonthlyBilling.php`, `app/Filament/Widgets/{MonthlyBillingPieChart,MonthlyBillingChart,MonthlyBillingMethodChart,MonthlyBillingDepositChart,MonthlyBillingSummary}.php`, `app/Filament/Widgets/Concerns/ReadsMonthlyBillingFilters.php`, `BillingService::{billingProgress,commissionTotal}()`, `resources/views/filament/modals/house-photo.blade.php`, `tests/Feature/MonthlyBillingPageTest.php` |
| Setoran Petugas — TASK-09 (form info-panel sisa real-time, officer_id terkunci utk petugas, received_by admin-only, widget ringkasan) | ✅ | `app/Filament/Resources/OfficerDeposits/`, `ScopeService::scopeDepositsForUser`, `tests/Feature/OfficerDepositModuleTest.php` |
| Dashboard — TASK-10 (5 stat cards, doughnut chart, tabel setoran petugas sisa merah, due-today per cluster, filter periode global via HasFiltersForm, scope petugas) | ✅ | `app/Filament/Pages/Dashboard.php`, `app/Filament/Widgets/`, `BillingService::monthlySummary(±officerId)`, `tests/Feature/DashboardWidgetTest.php` |
| Import Excel — TASK-11 (phpspreadsheet langsung — maatwebsite tidak support PHP 8.5; cleaning WA scientific→62xxx, paket×1000→harga, status fallback; header action admin-only) | ✅ | `app/Imports/CustomerImport.php`, `ListCustomers::getHeaderActions()`, `tests/fixtures/sample_pelanggan.xlsx`, `tests/Feature/CustomerImportTest.php` |
| Setoran Petugas — revisi periode & target (di luar TASK: filter **bulan-tahun** di halaman list (`HasFiltersForm` di `ListOfficerDeposits`; `InteractsWithTable` ada di parent `ListRecords` jadi **tidak** perlu `insteadof`), urutan konten filter → kartu ringkasan + panel per petugas → tabel, baris tabel ikut periode via override `getTableQuery()`. **`BillingService::officerProgress()`** = satu sumber 4 angka per petugas: `target` (**Harus Ditarik = tagihan pelanggan di cluster-nya − yang dibayar transfer**; transaksi transfer `officer_id` NULL sehingga wajib lewat relasi customer→cluster), `collected` (tunai masuk), `deposited`, `remaining` (= `officerRemainingBalance()` yang kini mendelegasi ke sini). Di-memo per petugas+periode di service singleton; `OfficerDeposit`/`Transaction` memanggil `forgetOfficerProgress()` di `saved`/`deleted` supaya tidak basi dalam satu request. Kolom **Harus Ditarik** di samping Nominal (tabel + export) lewat `OfficerDeposit::mustCollect()`; `OfficerDepositWidget` jadi 4 kolom dan ikut dipakai di dashboard) | ✅ | `BillingService::officerProgress()`, `app/Filament/Resources/OfficerDeposits/{Pages/ListOfficerDeposits,Tables/OfficerDepositsTable,Widgets/OfficerDepositSummary}.php`, `app/Filament/Widgets/OfficerDepositWidget.php`, `app/Models/{OfficerDeposit,Transaction}.php`, `tests/Feature/OfficerDepositModuleTest.php` |
| Komisi — `Penagihan` (di luar TASK: halaman hitungan turunan, **tanpa tabel baru**. Filter periode di atas kartu & tabel (pola sama dengan Tagihan Bulanan, termasuk `insteadof` untuk bentrok `normalizeTableFilterValuesFromQueryString`). **Basis komisi = `paid_amount` transaksi berstatus LUNAS** milik pelanggan yang direferalkan pada periode itu, dikali `commission_percent` penerima; **semua** penerima tampil, yang tanpa transaksi jadi 0. Query dipakai bersama tabel+kartu lewat `BillingService::commissionQuery()` (`withSum`/`withCount` atas `CommissionRecipient::referredTransactions()` — HasManyThrough). Total periode ada di subheading halaman. Kolom nama & WA mirror di-share dari `CommissionRecipientsTable::nameColumn()/whatsappColumn()`. `Transaction::scopeForPeriod()` kini `qualifyColumn` karena ikut dipakai di join. CEO+admin only. **Revisi (di luar TASK): chart dihapus, diganti 3 stat card** — Total Komisi (`commissionTotal`), **Estimasi Komisi** = tagihan pelanggan referal `billable()` yang **belum lunas** periode itu × persentase penerima (`estimated_base` di `commissionQuery()` → `CommissionRecipient::estimated_commission_amount` → `commissionEstimateTotal()`; juga jadi kolom tabel + export), dan jumlah penerima Non Pelanggan / Pelanggan. Penerima non-aktif tetap ikut dihitung di ketiga kartu, sama seperti tabelnya. Export Excel lewat `ExportTableAction` — ikut filter periode halaman + filter/search/sort tabel) | ✅ | `app/Filament/Pages/Commission.php`, `app/Filament/Widgets/CommissionSummary.php`, `BillingService::{commissionQuery,commissionEstimateTotal}()`, `CommissionRecipient::{referredTransactions,commission_amount,estimated_commission_amount}`, `tests/Feature/CommissionPageTest.php` |
| Penerima Komisi + Referal pelanggan — `Master` (di luar TASK: master penerima komisi, jenis **Pelanggan / Non Pelanggan**. Jenis Pelanggan = **MIRROR**, bukan salinan: kolom `name`/`address`/`whatsapp_number` sengaja NULL, datanya dibaca hidup dari `customers` lewat `display_name`/`display_address`/`display_whatsapp` — ganti data pelanggan, penerima ikut. Field form-nya tetap tampil tapi `disabled` + `dehydrated(false)`; `EditCommissionRecipient::mutateFormDataBeforeFill()` mengisinya untuk tampilan. Relasi `customer()` pakai **`withoutGlobalScopes()`** supaya label opsi Referal tidak kosong saat petugas membuka form pelanggan (global scope cluster di `Customer::booted()`). `commission_percent` default dari `Setting::defaultCommissionPercent()` tapi **tersimpan per penerima** — ubah Pengaturan tidak menyentuh penerima lama. Pelanggan dapat `referral_id` nullable (`nullOnDelete`); opsinya hanya penerima aktif dan **bukan dirinya sendiri**. Akses super_admin+admin; petugas tetap boleh memilih Referal di form pelanggan. **Panel "Pelanggan Direferalkan"** (RelationManager atas `referredCustomers`, muncul di halaman Edit karena resource ini tanpa View page): **Tambah Referal** (`AssociateAction`, opsi dibatasi `associateOptions()` = pelanggan tanpa `referral_id` dan **bukan** pelanggan mirror penerima itu sendiri) dan **Lepas Referal** (`DissociateAction` — `referral_id` jadi NULL, **pelanggannya tidak dihapus**; modal konfirmasi bawaan + `modalDescription` yang menegaskan itu). Wajib `protected static ?string $inverseRelationship = 'referral'` — Filament menebak relasi balik dari nama model induk (`commissionRecipient`) yang tidak ada di `Customer`) | ✅ | `app/Models/CommissionRecipient.php`, `app/Enums/RecipientType.php`, `app/Filament/Resources/CommissionRecipients/`, `app/Policies/CommissionRecipientPolicy.php`, migrations `2026_07_31_00001{0,1}`, `Customers/{Schemas/CustomerForm,Tables/CustomersTable,Pages/ListCustomers}.php`, `tests/Feature/{CommissionRecipientModuleTest,CustomerModuleTest}.php` |
| Pengaturan — sub-menu `Master` (di luar TASK: tabel `settings` **key-value** — setting baru = satu baris data, bukan migration baru; fallback `Setting::DEFAULTS` supaya DB fresh tetap benar tanpa seeder; **super_admin only** + guard `abort_unless` di `save()`. Isi saat ini hanya **komisi default 4%**; `Setting::defaultCommissionPercent()` dipakai sebagai nilai awal `commission_percent` di modul Penerima Komisi. Halaman = Filament Page ber-form via `content()` + `Form`/`EmbeddedSchema`/`InteractsWithFormActions`, bukan Resource) | ✅ | `app/Models/Setting.php`, `app/Filament/Pages/Settings.php`, migration `2026_07_31_000009`, `tests/Feature/SettingsPageTest.php` |
| Export Excel per tabel (di luar TASK: header action "Export Excel" di Tagihan Bulanan, Pelanggan, Setoran Petugas, Transaksi; mengekspor query yang sedang tampil — filter/search/sort tabel ikut — nominal ditulis sebagai angka mentah supaya bisa di-SUM; CEO+admin only) | ✅ | `app/Filament/Actions/ExportTableAction.php`, `app/Exports/Xlsx.php`, `{ListCustomers,ListTransactions,ListOfficerDeposits}::getHeaderActions()`, `MonthlyBilling::getHeaderActions()` |
| Laporan — TASK-12 (**Excel-only, PDF ditunda**; 3 sub-menu laporan terpisah di grup `Reports`: Rekap Pembayaran, Setoran Petugas, Tunggakan; **filter rentang tanggal** default bulan berjalan, basis `paid_at`/`deposited_at` via `BillingService::rangeSummary`; CEO+admin only. Default app locale kini `id`) | ✅ | `app/Filament/Pages/{AbstractRangeReportPage,PaymentRecapReport,OfficerDepositReport,ArrearsReport}.php`, `resources/views/filament/pages/report-range.blade.php`, `app/Exports/`, `app/Services/BillingService.php`, `tests/Feature/ReportPageTest.php` |

| Laporan lanjutan + fix tombol export (di luar TASK: **3 laporan baru** di grup `Reports` — **Komisi** (`BillingService::commissionRangeQuery()`, basis `paid_at` rentang tanggal, **tanpa** `estimated_base` yang hanya bermakna per-periode; total rentang tampil di `description()` halaman), **Per Cluster** (`Cluster::transactions()` HasManyThrough; tagihan = `sum(billing_amount)` billable × jumlah bulan dalam rentang, collection rate, jumlah penunggak), **Pelanggan** (baru/isolir/berhenti per bulan + total terdaftar kumulatif; blok per-paket **hanya di Excel** lewat override `previewRows()`, pola sama `OfficerDepositReport`). **2 bug fix:** (1) tombol Download Excel tidak pernah ter-render karena blade memakai slot `footerActions` — komponen `x-filament::section` hanya punya slot **`footer`**; (2) **paginasi tabel laporan tidak pernah aktif** — tabel ber-`records()` array tidak paginasi sendiri, Filament hanya menyuntik `page`/`recordsPerPage` dan closure-nya wajib mengembalikan `LengthAwarePaginator` (lihat `AbstractRangeReportPage::paginateRecords()`; `array_slice` dengan `preserve_keys` supaya record key unik lintas halaman, opsi `all` → perPage minimal 1). Laporan berbaris tetap & sedikit mematikan paginasi lewat `isPaginated()` false (Rekap Pembayaran = 8 metrik, Setoran Petugas = 1 baris/petugas); saat mati, `records()` **wajib** memakai closure tanpa parameter — Filament menyuntik `recordsPerPage` null lewat closure bertipe `int|string` miliknya sendiri sehingga meminta parameter itu = TypeError 500. Rekap Pembayaran kini sengaja **view-only** via `hasExport()` (guard `abort_unless` di `download()`, bukan sekadar tombol disembunyikan). **`terminated_at`** ditambahkan ke `customers`; tanggal isolir & berhenti kini di-stamp di `Customer::booted()` (bukan lagi di quick action) supaya form/import ikut tercatat — nilai eksplisit tidak ditimpa (`isDirty` guard) | ✅ | `app/Filament/Pages/{CommissionReport,ClusterReport,CustomerGrowthReport,AbstractRangeReportPage,PaymentRecapReport}.php`, `app/Exports/{CommissionReportExport,ClusterReportExport,CustomerGrowthExport}.php`, `resources/views/filament/pages/report-range.blade.php`, `app/Models/{Customer,Cluster}.php`, migration `2026_08_16_000001`, `tests/Feature/{ReportPageTest,CustomerModuleTest}.php` |
| Dashboard bisnis CEO (di luar TASK: `BusinessStatsOverview` — Total Komisi (`commissionTotal`), Pelanggan Baru, Isolir/Berhenti — dan `RevenueTrendChart` — bar **stacked tunai vs transfer 12 bulan** dari `monthlySummary`, ikut filter periode. Sengaja **bukan** "tagihan vs terbayar": `billing_amount` disimpan di pelanggan (nilai hari ini) sehingga sisi tagihan akan datar & menyesatkan untuk bulan lampau. Keduanya CEO+admin via `canView()`) | ✅ | `app/Filament/Widgets/{BusinessStatsOverview,RevenueTrendChart}.php`, `app/Filament/Pages/Dashboard.php`, `tests/Feature/DashboardWidgetTest.php` |
| Tema "NOC Console" (di luar TASK; custom Filament theme + Tailwind 4) — dark-first, palet primary cyan + gray navy, wordmark teks bergradient, favicon SVG, backdrop login, chip periode di topbar, strip status jaringan, stat card berikon + sparkline | ✅ | `resources/css/filament/admin/theme.css`, `app/Providers/Filament/AdminPanelProvider.php`, `app/Filament/Widgets/NetworkStatusStrip.php`, `resources/views/filament/{brand,login-backdrop,topbar-status}.blade.php`, `tests/Feature/{PanelThemeTest,NetworkStatusStripTest}.php` |
| Urutan sidebar (di luar TASK: `$navigationSort` eksplisit di tiap resource/page grup `Billing` — Tagihan Bulanan, Pelanggan, Setoran Petugas, Transaksi, Komisi — dan grup `Master` — Pengguna, Paket, Cluster, Penerima Komisi, Pengaturan — sebelumnya jatuh ke urutan alfabetis label terjemahan. Grup `Master` diberi label tampilan "Master Data"; menu Pelanggan diberi `getNavigationLabel()` terpisah "Data Pelanggan" di sidebar, judul halaman/breadcrumb tetap "Pelanggan") | ✅ | `lang/id.json`, `app/Filament/Resources/Customers/CustomerResource.php` |
| Popup pasca-simpan di Buat Transaksi, Setoran Petugas & Pelanggan (di luar TASK: tombol **"Buat & buat lainnya" dihapus** — pilihan lanjut/berhenti dipindah ke **popup setelah tersimpan**: "Kembali ke Daftar" atau "Buat Data Lagi". Mekanismenya `afterCreate()` → `mountAction('createdPrompt')` lalu **`throw new Halt`** untuk menahan redirect bawaan `CreateRecord`; `Halt` default tidak me-rollback sehingga record tetap ter-commit — konsekuensinya notifikasi hijau bawaan tidak terkirim (digantikan modal). Dua tombol modal = satu `->action()` yang sama dengan `$arguments` berbeda lewat `extraModalFooterActions()` + `makeModalSubmitAction('another', ['another' => true])`; "Buat Data Lagi" **redirect ke halaman create fresh**, bukan reset schema manual. Modal sengaja tidak bisa ditutup (`modalCloseButton(false)`, tanpa cancel, tanpa klik-luar/ESC) supaya user tidak balik ke form terisi lalu submit ganda. "Kembali" membawa `?created=<id>`; list page **memin record itu ke baris pertama** lewat `orderByRaw('id = ? desc')` di `getTableQuery()` — sort tabel jadi tie-breaker karena Filament hanya meng-`orderBy()` di belakang query kita. Pin lepas sendiri saat user menyentuh tabel (query string hilang di request Livewire). **Alur "Catat Pembayaran" dari Tagihan Bulanan (`?customer_id=`) dikecualikan** via `shouldPromptAfterCreate()` — tetap langsung balik ke Tagihan Bulanan. Filter default list sengaja **tidak** diikutkan, jadi record berperiode di luar filter memang tak terlihat. Catatan test: setelah popup termount, `assertHasNoFormErrors()` **error** (`mountedActionSchema0` tidak ada karena action ini tanpa schema) — pakai `assertHasNoErrors()`. Tabel Pelanggan tanpa `defaultSort` — urut ULID naik, jadi tanpa pin record baru justru mendarat di halaman terakhir) | ✅ | `app/Filament/Concerns/PromptsAfterCreate.php`, `app/Filament/Resources/{Transactions,OfficerDeposits,Customers}/Pages/{Create,List}*.php`, `lang/id.json`, `tests/Feature/{TransactionModuleTest,OfficerDepositModuleTest,CustomerModuleTest}.php` |
| Kolom WhatsApp sebagai Text di semua export (di luar TASK: nomor WA adalah string berisi angka; `DefaultValueBinder` phpspreadsheet mengubahnya jadi float sehingga Excel menampilkan `6.28123E+11`. Diperbaiki **di satu tempat** — `Xlsx::stream()` yang dipakai semua export — dengan `StringValueBinder` bawaan phpspreadsheet ber-`setNumericConversion(false)`: string ditulis `TYPE_STRING`, angka (nominal sudah di-cast `(float)`/`(int)` di pemanggil) tetap numerik & bisa di-SUM; `setSetIgnoredErrors(true)` mematikan segitiga hijau "number stored as text". Binder itu **state global statis** → dikembalikan ke default di `finally`. Kolom yang diberi `FORMAT_TEXT` **dideteksi dari baris heading** yang sama dengan `__('WhatsApp')`, jadi 5 call site (2 export laporan + 3 export tabel) dan export baru ikut otomatis tanpa parameter tambahan. Sekalian `fromArray(..., strictNullComparison: true)` — sebelumnya nilai `0` dan `''` dilewati diam-diam sehingga nominal 0 keluar sebagai sel kosong) | ✅ | `app/Exports/Xlsx.php`, `tests/Feature/ExportFormatTest.php` |
| Lupa Password via email (di luar TASK: `->passwordReset()` di panel — halaman request/reset, link di login, notifikasi, dan tabel `password_reset_tokens` semuanya bawaan Filament/Laravel, tanpa model/migration/route baru. User soft-deleted otomatis tidak ketemu broker (global scope); user `is_active = false` **boleh** reset tapi tetap ditolak `canAccessPanel()` saat login — sengaja tidak diberi guard tambahan. Body email memakai key JSON Laravel → diterjemahkan lewat `lang/id.json` (UI halamannya sudah Indonesia dari paket bahasa Filament). **Ops:** notifikasi Filament itu `ShouldQueue` dan `QUEUE_CONNECTION=database`, jadi **wajib ada queue worker** — snippet supervisor didokumentasikan di `README-Docker.md`, deploy menjalankan `queue:restart`. `.env` produksi wajib SMTP asli + `APP_URL` domain asli (tautan reset dibangun dari `APP_URL`)) | ✅ | `app/Providers/Filament/AdminPanelProvider.php`, `lang/id.json`, `.github/workflows/deploy.yml`, `README-Docker.md`, `tests/Feature/PasswordResetTest.php` |
| Audit Log (di luar TASK: jejak aktivitas per user — **super_admin only**, append-only, read-only di UI. Sadapannya **wildcard event Eloquent** (`Event::listen('eloquent.updated: *')`) di `AuditService::listen()`, **bukan trait per model**: semua jalur tulis (form, quick action ISOLIR, import Excel, seeder) lewat `Model::save()` sehingga tidak ada jalur yang bisa lupa dipasangi — satu titik menutup perubahan status & input pembayaran sekaligus. Denylist hanya `AuditLog` sendiri (cegah rekursi); model baru otomatis teraudit. **Aktor wajib ada** — `record()` return early saat `auth()->id()` null, jadi seeder/artisan/factory tidak menghasilkan baris. Diff `Updated` dari `getChanges()` + `getOriginal()`, `Created` dari `getAttributes()`; `password`/`remember_token`/timestamp dibuang lewat `getHidden()` **dan** konstanta `SKIP_FIELDS`. Akses menu lewat `LogPageVisit` di **`authMiddleware`** (bukan `middleware()`: butuh user login, dan request Livewire `/livewire/update` bukan route panel sehingga tidak spam), dicatat **setelah response** & hanya GET sukses (403/redirect bukan "akses menu"), **dedupe 5 menit** per user+path via `Cache::add` atomik. Login/Logout dari event Auth bawaan — `Logout` **wajib** ambil aktor dari `$event->user`, `auth()->id()` sudah null. Export dicatat satu baris di `ExportTableAction` → menutup semua call site. Retensi **6 bulan** via `MassPrunable` + `model:prune` harian di `routes/console.php` — **butuh cron `schedule:run`**, didokumentasikan di `README-Docker.md`. **Dua jebakan yang sudah kena:** (1) kolom **tidak boleh** bernama `changes` — Eloquent punya properti internal `protected $changes`, sehingga `$this->changes` di dalam model membaca dirty-tracking dan bukan kolomnya (senyap); dinamai `changed_values`. (2) kolom JSON di tabel Filament punya state **array** dan `formatStateUsing()` dipanggil **per elemen** → ringkasan keluar berulang; pakai `->state()`. `subject_id` sengaja `string` bukan `ulidMorphs()` (subjek bisa ULID, bigint `users`, atau null); `url` hanya diisi untuk akses menu; `subject_type` sengaja null untuk akses menu (isinya class Page, basename-nya tak bisa diterjemahkan)) | ✅ | `app/Services/AuditService.php`, `app/Models/AuditLog.php`, `app/Enums/AuditEvent.php`, `app/Http/Middleware/LogPageVisit.php`, `app/Policies/AuditLogPolicy.php`, `app/Filament/Resources/AuditLogs/`, `app/Filament/Actions/ExportTableAction.php`, migration `2026_08_18_000001`, `routes/console.php`, `README-Docker.md`, `tests/Feature/AuditLogTest.php` |
| Panel mobile Petugas `/petugas` (di luar TASK: **panel Filament kedua** khusus `field_officer`, desainnya dari mockup `docs/design/petugas-mobile-mockup.html`. **Petugas kini diblokir dari `/admin` dan admin/super_admin dari `/petugas`** — `User::canAccessPanel()` bercabang `match ($panel->getId())`; `/` redirect sadar-role. Panel **tanpa discovery** (halaman & resource didaftarkan eksplisit) supaya resource admin tidak ikut terdaftar, `->navigation(false)` + bottom nav 3 menu lewat render hook `BODY_END`, brand memakai logo topbar **bawaan** (`.fi-topbar-start`, yang default-nya hanya tampil >=64rem → di tema field dibuat `display:flex` di semua ukuran; **jangan** menambah brand lewat render hook `TOPBAR_START`, itu bikin logo dobel di desktop karena logo bawaan ikut muncul. Style `.fi-brand*` tetap **wajib disalin** ke tema field). **Search global dimatikan** lewat `$isGloballySearchable = false` di `FieldCustomerResource` (requirement), pencarian nama ada di dalam list. Menu: **Dashboard** (3 stat: jatuh tempo hari ini · Cash di Tangan = `officerProgress()['remaining']` · belum bayar; list due-today yang **belum lunas bulan ini** + tombol WA/Maps/Foto/Tagih), **Billing** (segmented Data Pelanggan | Transaksi — keduanya kartu `contentGrid(1)`; Pelanggan ber-search nama + filter status & due-today; **Transaksi = daftar tagihan per periode** pola `MonthlyBilling`, bukan daftar transaksi tercatat, karena "belum bayar" tidak punya record transaksi — filter Periode bebas + Status Bayar, tombol Catat Pembayaran hanya di baris belum lunas), **Pengaturan** (profil, cluster view-only via `ScopeService` — sengaja Page bukan Resource karena `ClusterPolicy` menolak petugas, riwayat setoran **read-only**, logout). Form **dipakai bersama** panel admin (`CustomerForm`/`TransactionForm`) — tidak di-fork; satu kolom di mobile lewat CSS, bukan bedah schema. **`referral_id` disembunyikan dari petugas** `visible()` **dan** `dehydrated()` (payload forged tidak tersimpan). **Titik koordinat** mengisi `maps_url` (tanpa kolom baru): `suffixAction` + `alpineClickHandler` memanggil `navigator.geolocation` lalu `$wire.$set(statePath, 'https://maps.google.com/?q=lat,lng')` — statePath diambil dinamis (`form.*` di sini, `data.*` di tempat lain) dan tempel link manual tetap jalan. Kamera cukup `FileUpload->image()` bawaan (picker mobile sudah menawarkan kamera). `CreateFieldTransaction` **extends** `CreateTransaction` supaya prefill `?customer_id`/`?period` (cek `instanceof`) tetap bekerja; `FieldTransactionResource` tanpa halaman index → `getIndexUrl()` diarahkan ke halaman Transaksi. **Login satu halaman untuk semua role** (`app/Filament/Auth/Login.php`, dipasang lewat `->login(Login::class)` di panel admin; panel field sengaja **tanpa** `->login()`/`->passwordReset()`): Filament mengevaluasi `canAccessPanel()` terhadap panel tempat form dirender **dan** `LoginResponse` menghitung URL-nya saat `toResponse()` — dua-duanya bikin login lintas panel mustahil apa adanya. Solusinya `panelIdFor(email)` menentukan panel tujuan, `parent::authenticate()` dijalankan dalam konteks panel itu, lalu **URL tujuan dihitung eager** (di dalam `try`, selagi panel tujuan aktif) dan dibungkus `LoginResponse` sendiri; panel dikembalikan di `finally` supaya render ulang saat kredensial salah tetap benar. `url.intended` hanya dihormati bila prefix-nya panel tujuan — deep link sisa dari panel lain akan berujung 403 tepat setelah login berhasil. Route: `/login` (bernama `login`, sekaligus **fallback wajib** Laravel karena panel field tak punya halaman login → `Filament::getLoginUrl()` null) dan `/petugas/login` sama-sama redirect ke `/admin/login`. **Konsekuensi yang disengaja:** petugas tidak lagi bisa mencatat setoran sendiri (tidak ada route-nya di mobile) walau `OfficerDepositPolicy::create` masih mengizinkan — policy sengaja tidak diubah agar alur lama bisa dipulihkan tanpa migrasi aturan) | ✅ | `app/Providers/Filament/FieldPanelProvider.php`, `app/Filament/Field/`, `resources/css/filament/field/theme.css`, `resources/views/filament/field/`, `app/Models/User.php`, `routes/web.php`, `vite.config.js`, `tests/Feature/FieldPanelTest.php` |
| Panduan Pengguna (di luar TASK: `docs/user-guide/` — 4 halaman HTML mandiri berbahasa Indonesia untuk **pengguna akhir**, bukan spesifikasi developer: `index.html` (flowchart alur sistem 3 lajur peran + urutan input data + matriks hak akses), `ceo.html`, `admin.html`, `petugas.html`, memakai satu `assets/guide.css` (light-first + `prefers-color-scheme` + `@media print` untuk PDF) dan **40 screenshot asli** di `screenshots/`. Screenshot diambil dengan Playwright — **tanpa devDependency baru**, resepnya di `docs/user-guide/README.md`. Data peragaannya dari **`DemoSeeder`** yang sengaja **tidak** dipanggil `DatabaseSeeder` (jalankan `php artisan db:seed --class=DemoSeeder`): tanpa ini Dashboard, Komisi, 6 laporan, dan Setoran Petugas tampil kosong karena seed standar tidak punya transaksi/setoran/penerima komisi. Seeder itu `Auth::login($admin)` dulu — `AuditService::record()` return early saat aktor null, jadi tanpa login Log Aktivitas tetap kosong; ia juga menjadwalkan tagihan **jatuh tempo hari ini yang belum lunas** per petugas (Dashboard petugas hanya menampilkan yang belum lunas, jadi tanpa ini contoh utama panduan petugas kosong) dan menulis foto rumah placeholder via GD. `faker_locale` diubah ke `id_ID` (di `config/app.php` **dan** `.env`/`.env.example` — env menimpa config) supaya nama & alamat pelanggan Indonesia. `tests/Feature/DemoSeederTest.php` menjaga tiap halaman yang didokumentasikan tetap berisi. **Terintegrasi ke menu aplikasi** lewat route `guide`/`guide.file` di `routes/web.php` (middleware `auth`, jadi panduan **tidak** ditaruh di `public/`): `/panduan` mengarahkan ke halaman sesuai peran, `/panduan/{file}` menyajikan berkasnya. Route itu `realpath` + cek awalan (menutup `../../.env`) dan **menetapkan Content-Type dari ekstensi** — `response()->file()` menebak MIME dari isi berkas dan mengirim CSS sebagai `text/plain` sehingga browser menolaknya dan panduan tampil tanpa gaya; daftar ekstensinya sekaligus menahan `README.md` yang memuat kredensial akun contoh. Menu: panel admin lewat `->navigationItems()` grup **Bantuan** (tab baru); panel petugas lewat kartu di halaman Pengaturan, karena bottom nav dikunci 3 menu. `docs/` ikut `COPY . ` di `Dockerfile` (tanpa `.dockerignore`), jadi tersedia di produksi. **Catatan pengambilan screenshot:** login Playwright gagal senyap bila halaman melewati dua redirect (`/petugas` → `/login` → `/admin/login`) — buka `/admin/login` langsung; login petugas dilakukan di viewport desktop lalu viewport diubah ke ukuran HP; rate limit login **5/menit**; bila form berhenti merespons setelah banyak navigasi, buat **context browser baru**) | ✅ | `docs/user-guide/`, `database/seeders/DemoSeeder.php`, `config/app.php`, `.env.example`, `tests/Feature/DemoSeederTest.php` |

**Spesifikasi modul billing ada di `docs/files/PionTech_AI_Agent_Tasks/`** (PRD_CONTEXT.html + TASK-01..16). Kerjakan sesuai urutan task & dependency di TASK-00. Selanjutnya: TASK-13.

### Catatan tema

- **Semua override CSS terkumpul di `resources/css/filament/admin/theme.css`** memakai CSS hook `fi-*` bawaan Filament v5 — jangan menyebar utility styling ke Blade. Token khusus tema diawali `--noc-*`.
- Warna stat card **tidak ada di elemen root**-nya; Filament hanya menaruh `fi-color-*` di description/chart. Garis aksen kartu membacanya lewat `:has()`. Konsekuensinya: stat tanpa description **dan** tanpa chart tidak akan berwarna.
- Widget dengan custom view **wajib** membungkus root-nya dengan `<x-filament-widgets::widget>` — komponen itulah yang menerapkan `$columnSpan` ke grid dashboard.
- Chart pakai `borderColor: 'transparent'`, bukan warna surface, supaya tidak jadi cincin gelap di light mode.

## Checklist Modul Baru

1. Tulis spec modul (format `sippw/.plan/modules/`: overview, schema, relationships, permissions).
2. Migration (ikuti konvensi kolom di atas).
3. Enum baru bila ada kolom pilihan → `app/Enums/` + `HasLabel`.
4. Model + relationships + casts.
5. `php artisan make:filament-resource X --generate` lalu rapikan Schemas/Tables.
6. Access gate (`canAccess`) + scoping (`getEloquentQuery` via ScopeService) sesuai matriks permission.
7. Feature test kontrol akses.
8. Update tabel "Implemented Modules" di file ini.
