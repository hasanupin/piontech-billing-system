version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: piontech_billing_app
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./:/var/www/html
      - ./docker/php/local.ini:/usr/local/etc/php/conf.d/local.ini
    ports:
      - "8002:80"
    networks:
      - piontech
    depends_on:
      - db
      - redis
    environment:
      - DB_CONNECTION=mysql
      - DB_HOST=db
      - DB_PORT=3306
      - DB_DATABASE=piontech_billing
      - DB_USERNAME=laravel_user
      - DB_PASSWORD=laravel_password

  db:
    image: mysql:8.0
    container_name: piontech_billing_db
    restart: unless-stopped
    tty: true
    ports:
      - "3309:3306"
    environment:
      MYSQL_DATABASE: piontech_billing
      MYSQL_USER: laravel_user
      MYSQL_PASSWORD: laravel_password
      MYSQL_ROOT_PASSWORD: root_password
      SERVICE_TAGS: dev
      SERVICE_NAME: mysql
    volumes:
      - dbdata:/var/lib/mysql
      - ./docker/mysql/my.cnf:/etc/mysql/my.cnf
    networks:
      - piontech

  redis:
    image: redis:alpine
    container_name: piontech_billing_redis
    restart: unless-stopped
    ports:
      - "6382:6379"
    networks:
      - piontech

networks:
  piontech:
    driver: bridge

volumes:
  dbdata:
    driver: local
